#include <stdio.h>
#include <stdio.h>
void main()
{
int s, i;
scanf("%d",&s); //number of test cases you want to run
for(i=1;i<=s;i++)
{
    int arr[100];
    int n;
    scanf("%d", &n);
    for(i=0; i<n; i++)
    {
    scanf("%d", &arr[i]);
    }
    

}
return 0;
}